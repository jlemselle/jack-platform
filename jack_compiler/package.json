{
  "name": "hack-assembler",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watchAll",
    "assemble": "ts-node src/assemble.ts",
    "interpret": "ts-node src/interpret.ts",
    "execute": "ts-node src/execute.ts",
    "start": "npm run compile",
    "compile": "ts-node src/compile.ts ./src/os && ts-node src/compile.ts ./src/app",
    "jackc": "ts-node src/compile.ts ./src/os && ts-node src/compile.ts ./src/app && ts-node src/interpret.ts ./dist && ts-node src/assemble.ts dist.asm",
    "jackr": "ts-node src/compile.ts ./src/os && ts-node src/compile.ts ./src/app && ts-node src/interpret.ts ./dist && ts-node src/assemble.ts dist.asm && ts-node src/execute.ts dist.pack"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.6",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
